#[cfg(test)]
mod tests {
    #[test]
    fn basic_math() {
        let my_sum = 2 + 2;
        assert_eq!(my_sum, 4);
    }

    #[test]
    fn basic_math2() {
        let my_sum = 3 + 2;
        assert_eq!(my_sum, 5);
    }
}
